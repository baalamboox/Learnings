document.querySelector("title").innerHTML = "JS | If else";

/**
 * What's a if?
 * Conditional statements are used to perform different actions based on different conditions.
 */

let user = "baalambooxs";

if(user === "baalamboox") {
    // All code here
    console.log("Success!");
} else if(user == "baalambooxs") {
    console.log("user semicorrect!");
} else {
    console.log("Icorrect user!");
}